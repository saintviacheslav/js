const a = prompt("enter:")
const b = prompt("enter:")
const c = prompt("enter:")
if (isNaN(a) && isNaN(b) && isNaN(b)) {
    console.log("\t" + a + "\t" + b + "\t" + c)
} else {
    console.log(`false`)
}